# Engineering

Use `profiles/engineering.json` only as an `UNKNOWN-until-specified` contract. Require the domain, standard, edition, units, and symbol references before drawing semantic components. Consult the official [ISO technical drawing catalog](https://www.iso.org/ics/01.100/x/) for the applicable domain standard.

Resolve all symbols and line classes through live MCP search or a verified licensed mapping. Treat port attachment and connection type as semantic.

Use `assets/templates/technical-landscape.xml` only for title, provenance, units, and legend placement. Geometry validation cannot certify engineering correctness or safety.
